import SocialValue from "@/legacy-pages/SocialValue";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Social Value & Community Impact | EntireFM",
  description: "Measurable social value delivery through facilities management. Local employment, apprenticeships, and community support.",
};

export default function SocialValuePage() {
  return <SocialValue />;
}
