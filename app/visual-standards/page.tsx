import VisualStandards from "@/legacy-pages/VisualStandards";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Visual Standards | EntireFM Branding & Identity",
  description: "Our visual identity and standards. Consistency in presentation and professional appearance across all EntireFM operations.",
};

export default function VisualStandardsPage() {
  return <VisualStandards />;
}
