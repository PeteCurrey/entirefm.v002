import WhySwitch from "@/legacy-pages/WhySwitch";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Why Switch to EntireFM | Facilities Management Transition",
  description: "Tired of excuses? Experience FM that performs. We handle the transition so you don't have to. No operational downtime, no compliance gaps.",
};

export default function WhySwitchPage() {
  return <WhySwitch />;
}
